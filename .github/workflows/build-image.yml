name: build-and-push

on:
  push:
    branches: ["dev", "prod"]

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}
          tags: |
            type=sha,prefix=${{ github.ref_name }}-sha-
            type=ref,event=tag
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Resolve deploy environment
        id: deploy
        run: |
          case "${GITHUB_REF_NAME}" in
            dev)
              echo "environment=dev" >> "${GITHUB_OUTPUT}"
              echo "app_base_path=/kopic-dev/" >> "${GITHUB_OUTPUT}"
              echo "ws_path=/kopic-dev/ws" >> "${GITHUB_OUTPUT}"
              echo "app_base_path_nginx=/kopic-dev" >> "${GITHUB_OUTPUT}"
              echo "image_tag=dev-sha-${GITHUB_SHA::7}" >> "${GITHUB_OUTPUT}"
              ;;
            prod)
              echo "environment=prod" >> "${GITHUB_OUTPUT}"
              echo "app_base_path=/kopic/" >> "${GITHUB_OUTPUT}"
              echo "ws_path=/kopic/ws" >> "${GITHUB_OUTPUT}"
              echo "app_base_path_nginx=/kopic" >> "${GITHUB_OUTPUT}"
              echo "image_tag=prod-sha-${GITHUB_SHA::7}" >> "${GITHUB_OUTPUT}"
              ;;
            *)
              echo "unsupported branch: ${GITHUB_REF_NAME}" >&2
              exit 1
              ;;
          esac

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VITE_APP_BASE_PATH=${{ steps.deploy.outputs.app_base_path }}
            VITE_WS_PATH=${{ steps.deploy.outputs.ws_path }}
            VITE_GE_ID=ge-local
            APP_BASE_PATH=${{ steps.deploy.outputs.app_base_path_nginx }}

      - name: Checkout infra repo
        uses: actions/checkout@v4
        with:
          repository: mohyeonMan/home-k3s-infra
          ref: main
          token: ${{ secrets.INFRA_REPO_TOKEN }}
          path: infra

      - name: Update kustomization tag
        run: |
          cd infra
          DEPLOY_ENV="${{ steps.deploy.outputs.environment }}"
          TARGET_FILE="apps/kopic/${DEPLOY_ENV}/client/kustomization.yaml"

          if [ ! -f "${TARGET_FILE}" ]; then
            echo "skip infra tag update because ${TARGET_FILE} does not exist yet"
            exit 0
          fi

          yq -i ".images[0].newTag = \"${{ steps.deploy.outputs.image_tag }}\"" "${TARGET_FILE}"

      - name: Commit and push
        run: |
          cd infra
          DEPLOY_ENV="${{ steps.deploy.outputs.environment }}"
          TARGET_FILE="apps/kopic/${DEPLOY_ENV}/client/kustomization.yaml"

          if [ ! -f "${TARGET_FILE}" ]; then
            echo "skip infra push because ${TARGET_FILE} does not exist yet"
            exit 0
          fi

          IMAGE_TAG="${{ steps.deploy.outputs.image_tag }}"
          REPO_NAME="${GITHUB_REPOSITORY##*/}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add "${TARGET_FILE}"
          git commit -m "deployment(${DEPLOY_ENV}): ${REPO_NAME} image tag [${IMAGE_TAG}]" || exit 0
          git push
